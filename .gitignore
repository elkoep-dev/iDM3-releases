# Nothing here is signed and no key is used, so any of these turning up would be a mistake
# rather than part of a workflow. Kept as a backstop: this repository is public, and a
# credential committed here is a credential published.
*.private.xml
*.key
*.pem
*.pfx
*.p12
secrets/

# Build output
_site/
artifacts/

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
.vs/
